/* =========================================================
   REDLINE AUTO CARE — main.js
   Main JavaScript that imports all modules
   ========================================================= */

// All modules are self-initializing, so we just need to ensure
// they're loaded. Each module checks document.readyState internally.

// Services data and booking modules are loaded via script tags
// in the HTML files where needed.